<?php include 'header.phtml'; ?>

<section class="hero">
    <div class="hero-content">
        <h1>TOUT SUR LA LEAGUE</h1>
        <p>Les players, les teams et l'actu des matchs</p>
    </div>
</section>

<main class="container">
    
    <div class="section-container">
        <h2 class="section-title text-yellow">LA TEAM À LA UNE</h2>
        
        <?php if (isset($featuredTeam) && $featuredTeam): ?>
            <div class="featured-layout">
                <div class="featured-left">
                    <a href="index.php?page=team&id=<?= $featuredTeam['id'] ?>">
                        <img src="<?= htmlspecialchars($featuredTeam['logo_url']) ?>" 
                             alt="<?= htmlspecialchars($featuredTeam['name']) ?>">
                    </a>
                </div>

                <div class="featured-right">
                    <h3 class="text-yellow"><?= htmlspecialchars($featuredTeam['name']) ?></h3>
                    <span class="featured-sub">Les players</span>

                    <?php if (!empty($featuredTeamPlayers)): ?>
                        <div class="mini-grid">
                            <?php foreach ($featuredTeamPlayers as $player): ?>
                                <div class="mini-card">
                                    <a href="index.php?page=player&id=<?= $player['id'] ?>">
                                        <img src="<?= htmlspecialchars($player['portrait_url']) ?>" 
                                             alt="<?= htmlspecialchars($player['nickname']) ?>">
                                    </a>
                                    <h4><?= htmlspecialchars($player['nickname']) ?></h4>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        <?php endif; ?>
    </div>

    <div class="section-container">
        <h2 class="section-title text-pink">LES PLAYERS À LA UNE</h2>
        
        <div class="grid-3">
            <?php if (isset($featuredPlayers) && !empty($featuredPlayers)): ?>
                <?php foreach ($featuredPlayers as $player): ?>
                    <div class="card player-card-home">
                        <a href="index.php?page=player&id=<?= $player['id'] ?>" style="width:100%;">
                            <img src="<?= htmlspecialchars($player['portrait_url']) ?>" 
                                 alt="<?= htmlspecialchars($player['nickname']) ?>">
                        </a>
                        <h3 class="text-pink"><?= htmlspecialchars($player['nickname']) ?></h3>
                        <p class="text-yellow player-team-name">
                            <?= htmlspecialchars($player['team_name'] ?? 'Free Agent') ?>
                        </p>
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>
    </div>

    <div class="section-container">
        <h2 class="section-title text-blue">LE DERNIER MATCH</h2>
        
        <?php if (isset($lastMatch) && $lastMatch): ?>
        <div class="match-card">
            
            <h3 class="match-title">
                <?= htmlspecialchars($lastMatch['team1_name']) ?> VS <?= htmlspecialchars($lastMatch['team2_name']) ?>
            </h3>
            
            <p class="match-date">
                <?= date("d/m/Y", strtotime($lastMatch['date'])) ?>
            </p>
            
            <div class="match-logos">
                <div class="match-box-img">
                     <img src="<?= htmlspecialchars($lastMatch['team1_logo']) ?>" alt="<?= htmlspecialchars($lastMatch['team1_name']) ?>">
                </div>

                <div class="match-box-img">
                     <img src="<?= htmlspecialchars($lastMatch['team2_logo']) ?>" alt="<?= htmlspecialchars($lastMatch['team2_name']) ?>">
                </div>
            </div>
            
        </div>
        <?php endif; ?>
    </div>

</main>
</body>
</html>