<?php include 'header.phtml'; ?>

<main class="container">
    <h2 class="text-pink">LES PLAYERS DE LA LEAGUE</h2>

    <div class="grid-3">
        <?php foreach ($players as $player): ?>
            
            <div class="card player-list-card">
                
                <img src="<?= htmlspecialchars($player['portrait_url']) ?>" 
                     alt="<?= htmlspecialchars($player['nickname']) ?>"
                     style="height: 300px; object-fit: cover; object-position: top;">
                     
                <h3><?= htmlspecialchars($player['nickname']) ?></h3>
                
                <p><?= htmlspecialchars($player['team_name'] ?? 'Free Agent') ?></p>
                
                <a href="index.php?page=player&id=<?= $player['id'] ?>" class="btn">
                    DÉCOUVREZ <?= strtoupper($player['nickname']) ?>
                </a>
            </div>
            
        <?php endforeach; ?>
    </div>
</main>
</body>
</html>