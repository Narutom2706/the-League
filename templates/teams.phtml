<?php include 'header.phtml'; ?>

<main class="container">
    <h2 class="text-yellow">LES TEAMS DE LA LEAGUE</h2>

    <div class="grid-3">
        <?php foreach ($teams as $team): ?>
            <div class="card team-card">
                <img src="<?= htmlspecialchars($team['logo_url']) ?>" 
                     alt="<?= htmlspecialchars($team['name']) ?>">
                     
                <h3 class="text-yellow"><?= htmlspecialchars($team['name']) ?></h3>
                <p><?= htmlspecialchars($team['description']) ?></p>
                
                <a href="index.php?page=team&id=<?= $team['id'] ?>" class="btn">
                    DÉCOUVREZ <?= strtoupper($team['name']) ?>
                </a>
            </div>
        <?php endforeach; ?>
    </div>
</main>
</body>
</html>